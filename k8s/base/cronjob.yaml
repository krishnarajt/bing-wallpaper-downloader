apiVersion: batch/v1
kind: CronJob
metadata:
  name: bing-wallpaper
spec:
  schedule: "0 */6 * * *"
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: bing-wallpaper
              image: ghcr.io/krishnarajt/bing-wallpaper-downloader:git-6caa45d3378e9d857b6bd546d1f75556c748f09b
              imagePullPolicy: IfNotPresent
              env:
                - name: MARKET
                  value: "en-IN"
                - name: OUTPUT_DIR
                  value: "/wallpapers"
              volumeMounts:
                - name: wallpaper-storage
                  mountPath: /wallpapers
          volumes:
            - name: wallpaper-storage
              hostPath:
                path: /srv/bing-wallpaper
                type: DirectoryOrCreate
